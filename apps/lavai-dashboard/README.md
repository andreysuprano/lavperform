# LavAI Dashboard

Painel administrativo interno LavPerform para gestão de agentes de IA (migrado de `agents-dashboard`).

```bash
yarn dev:lavai-dashboard
```

API padrão: `http://localhost:3000` (`NEXT_PUBLIC_API_URL`).
